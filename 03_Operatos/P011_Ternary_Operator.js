// Program to check if a number is even or odd using the ternary operator
let num = 10;
let result = (num % 2 === 0) ? "Even" : "Odd"
console.log(result)

// Program to check eligibility based on age
let age = 17;
let isEligible = (age >=18) ? "Eligible to Vote" : "Not Eligible for Vote"
console.log(isEligible)

// Finding the Maximum of Two Numbers
let num1 = 20;
let num2 = 25;
console.log((num1 > num2) ? num1 : num2)

// // Simple program for a basic grading system using nested ternary
let marks = 40;
let grade = (marks >= 90 ) ? "A":
            (marks >= 80 ) ? "B":
            (marks >= 70 ) ? "C":
            (marks >= 50 ) ? "D":
            "Fail"
console.log(grade)

